# B01 — Planner / раздел «Задачи»

Статусы: `[x]` готово, `[~]` в работе, `[ ]` запланировано. Этот файл — функциональный checklist раздела «Задачи». Календарь, Eisenhower, Focus, привычки и статистика ведутся отдельно.

## 1. Навигация

- `[x]` раскрываемый пункт «Задачи»;
- `[x]` Сегодня, Завтра, Следующие 7 дней, Все задачи, Входящие;
- `[x]` пользовательские списки с цветом и CRUD;
- `[x]` раскрываемые фильтры и метки со счётчиками;
- `[x]` «Сводка»: прогресс недели, завершения по дням, просроченные, нагрузка по спискам;
- `[x]` сохранённые пользовательские smart-фильтры (вид, поиск, список, метка, фильтр).

## 2. Список задач

- `[x]` секции «Просрочено», «Запланировано», «Без даты», «Выполнено»;
- `[x]` сворачивание каждой секции;
- `[x]` поиск, сортировка и группировка;
- `[x]` ручной порядок с drag-and-drop;
- `[x]` завершение и возврат задачи;
- `[x]` избранное;
- `[x]` четыре уровня приоритета: P1, P2, P3, P4;
- `[x]` массовый выбор и массовые действия: статус, приоритет, список, удаление;
- `[x]` меню действий: сегодня, завтра, входящие, дублирование, удаление;
- `[ ]` клавиатурное перемещение порядка задач;
- `[x]` одноступенчатый Undo после изменения, удаления или массового действия.

## 3. Быстрое добавление

- `[x]` название задачи;
- `[x]` дата;
- `[x]` приоритет;
- `[x]` список;
- `[x]` создание клавишей Enter и фокус клавишей `N`;
- `[x]` метки прямо из quick add;
- `[ ]` natural-language parsing даты и времени;
- `[ ]` создание из шаблона;
- `[x]` настройка видимых полей quick add с сохранением в браузере.

## 4. Карточка задачи

- `[x]` название и описание;
- `[x]` дата, начало, конец и длительность;
- `[x]` приоритет и список;
- `[x]` метки и их подсказки;
- `[x]` повторение, привычка, входящие и связанная цель;
- `[x]` добавление и завершение подзадач;
- `[x]` добавление метаданных вложений;
- `[x]` удаление задачи с подтверждением;
- `[x]` переименование, удаление и ручной порядок подзадач;
- `[~]` удаление вложения готово; загрузка самого файла появится после backend;
- `[ ]` напоминания, локация, участники и комментарии;
- `[ ]` история изменений задачи.

## 5. Данные и надёжность

- `[x]` localStorage repository;
- `[x]` миграция v1 → v2;
- `[x]` миграция v2 → v3 с сохранением задач;
- `[x]` нормализация повреждённых записей;
- `[x]` многоуровневый undo/redo: последние 50 изменений задач/списков в текущей сессии, клавиатура и кнопки;
- `[ ]` Auth + PostgreSQL + синхронизация устройств;
- `[ ]` offline queue и разрешение конфликтов.

## 6. AI внутри задач

- `[x]` предусмотрена контекстная AI-зона в выбранной задаче;
- `[ ]` обсуждение задачи в чате;
- `[ ]` разбиение задачи на подзадачи;
- `[ ]` оценка времени, риска и приоритета;
- `[ ]` preview → confirm → apply → undo;
- `[ ]` серверный gateway и fallback провайдера.

## Ближайший пакет

1. Шаблоны задач и естественный ввод даты.
2. Клавиатурное перемещение задач и адаптивная проверка.
3. Шаблоны и обработка естественного ввода даты.
4. Финальная полировка раздела «Задачи»; серверные возможности остаются отдельным этапом.
